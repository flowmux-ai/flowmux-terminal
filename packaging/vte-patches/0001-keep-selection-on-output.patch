--- a/src/vte.cc
+++ b/src/vte.cc
@@ -4006,19 +4006,11 @@
                 if (m_scroll_on_output || context.m_bottom || m_screen == &m_alternate_screen) {
                         scroll_to_bottom();
                 }
-                /* Deselect the current selection if its contents are changed
-                 * by this insertion. */
-                if (!m_selection_resolved.empty()) {
-                        //FIXMEchpe: this is atrocious
-                        auto selection = g_string_new(nullptr);
-                        get_selected_text(selection);
-                        if ((selection == nullptr) ||
-                            (m_selection[vte::to_integral(vte::platform::ClipboardType::PRIMARY)] == nullptr) ||
-                            (strcmp(selection->str, m_selection[vte::to_integral(vte::platform::ClipboardType::PRIMARY)]->str) != 0)) {
-                                deselect_all();
-                        }
-                        g_string_free(selection, TRUE);
-                }
+                /* flowmux patch: upstream VTE deselects whenever the foreground
+                 * application rewrites the cells under an active selection, which
+                 * destroys a text selection every time a TUI (Codex, Claude Code, vim,
+                 * ...) repaints. Keep the selection anchored across output instead,
+                 * matching Ghostty. See flowmux packaging/vte-patches. */
         }
 
         if (context.m_modified || (m_screen != context.m_saved_screen)) {
